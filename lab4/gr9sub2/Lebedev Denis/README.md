# Лабораторная работа №4: Системные вызовы

## Воспроизводимость

### Сборка

Для сборки библиотеки `libsyscall_spy.so` и тестового статического исполняемого файла `hello_static` выполните:

```bash
make
```

### Запуск экспериментов

Для запуска экспериментов и сохранения их вывода в каталог `logs/` используйте следующие команды:

```bash
# Эксперимент с ls
make run_ls

# Эксперимент с cat
make run_cat

# Эксперимент с grep
make run_grep

# Эксперимент со статической программой
make run_static_test
```

### Очистка

Для удаления сгенерированных файлов и логов выполните:

```bash
make clean
```
