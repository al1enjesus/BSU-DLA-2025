### Инструкции быстрого запуска

1. Подготовка:
```bash 
sudo apt update && sudo apt install -y \
  build-essential gcc gdb strace ltrace \
  linux-tools-common linux-tools-generic perf
```

2. Запуск:
```bash
bash ./run.sh
```
