# Отчёт по лабораторной работе 6: Файловые системы FUSE

**Студент:** Леончик Владислав
**Группа:** 9
**Подгруппа:** 2
**Вариант:** Только задание A

---

## 1. Цель работы

Изучить архитектуру Virtual File System (VFS) Linux и научиться создавать пользовательские файловые системы с использованием FUSE (Filesystem in Userspace). Реализовать базовую passthrough файловую систему, которая проксирует все операции в существующую директорию с логированием, и провести анализ производительности userspace файловых систем.

---

## 2. Теоретическая часть

### 2.1 Архитектура VFS и роль FUSE

**Virtual File System (VFS)** — это абстрактный слой в ядре Linux, предоставляющий единый интерфейс для работы с различными типами файловых систем (ext4, btrfs, NFS, FUSE и др.). VFS позволяет приложениям использовать одни и те же системные вызовы независимо от типа файловой системы.

**FUSE (Filesystem in Userspace)** позволяет создавать файловые системы в пространстве пользователя без написания кода для ядра. Драйвер FUSE в ядре перенаправляет VFS операции в userspace процесс через специальный протокол.

### 2.2 Схема взаимодействия

```
┌──────────────────────────────────────────────────────┐
│  Приложение (cat, ls, echo)                          │
│  Использует стандартные системные вызовы:            │
│  open(), read(), write(), stat()                     │
└────────────────────┬─────────────────────────────────┘
                     ↓
┌────────────────────────────────────────────────────────┐
│  VFS (Virtual File System) — ядро Linux                │
│  Единый интерфейс для всех файловых систем             │
└────────────────────┬───────────────────────────────────┘
                     ↓
┌────────────────────────────────────────────────────────┐
│  FUSE kernel module (/dev/fuse)                        │
│  Перенаправляет запросы в userspace                    │
└────────────────────┬───────────────────────────────────┘
                     ↓ (протокол FUSE)
┌────────────────────────────────────────────────────────┐
│  libfuse (userspace библиотека)                        │
│  Парсит запросы и вызывает callback функции            │
└────────────────────┬───────────────────────────────────┘
                     ↓
┌────────────────────────────────────────────────────────┐
│  Наша программа (myfuse)                               │
│  Реализует file operations: getattr, read, write, etc. │
│  Проксирует запросы в реальную файловую систему        │
└────────────────────────────────────────────────────────┘
```

### 2.3 Реализованные file operations

В рамках лабораторной работы были реализованы следующие операции:

| Операция   | Системный вызов | Описание                                      |
|------------|-----------------|-----------------------------------------------|
| `getattr`  | `stat()`        | Получение метаданных файла (размер, права)    |
| `readdir`  | `readdir()`     | Чтение содержимого директории                 |
| `open`     | `open()`        | Открытие файла                                |
| `read`     | `read()`        | Чтение данных из файла                        |
| `write`    | `write()`       | Запись данных в файл                          |
| `create`   | `open(O_CREAT)` | Создание нового файла                         |
| `unlink`   | `unlink()`      | Удаление файла                                |
| `mkdir`    | `mkdir()`       | Создание директории                           |
| `rmdir`    | `rmdir()`       | Удаление директории                           |
| `release`  | `close()`       | Закрытие файлового дескриптора                |

**Ключевая особенность passthrough FS:** Все операции проксируются в базовую директорию (`source_dir`), файлы реально хранятся на диске, FUSE лишь перехватывает обращения и логирует их.

---

## 3. Ход выполнения

### 3.1 Задание A: Passthrough FUSE Filesystem

#### 3.1.1 Структура проекта

```
lab6/
├── src/
│   ├── myfuse.c          # Точка входа программы
│   ├── operations.c      # Реализация FUSE операций
│   ├── operations.h      # Заголовочный файл
│   └── utils.c           # Вспомогательные функции (логирование)
├── Makefile              # Сборка проекта
└── REPORT.md             # Данный отчёт
```

#### 3.1.2 Ключевые компоненты реализации

**1. Преобразование путей (utils.c)**

Функция `get_full_path()` преобразует FUSE путь (например, `/test.txt`) в абсолютный путь реальной файловой системы (`/tmp/fuse_source/test.txt`):

```c
void get_full_path(char *fullpath, const char *path) {
    if (strcmp(path, "/") == 0) {
        strcpy(fullpath, source_dir);
        return;
    }
    
    size_t len = strlen(source_dir);
    if (source_dir[len - 1] == '/') len--;
    
    strncpy(fullpath, source_dir, len);
    fullpath[len] = '\0';
    strcat(fullpath, path);
}
```

**2. Логирование операций (utils.c)**

Каждая операция логируется в формате `[YYYY-MM-DD HH:MM:SS] OPERATION: path (result: N)`:

```c
void log_operation(const char *op_name, const char *path, int result) {
    time_t timer;
    char buffer[26];
    struct tm* tm_info;

    time(&timer);
    tm_info = localtime(&timer);
    strftime(buffer, 26, "%Y-%m-%d %H:%M:%S", tm_info);

    fprintf(stderr, "[%s] %s: %s (result: %d)\n", 
            buffer, op_name, path, result);
}
```

**3. Пример реализации операции (operations.c)**

Операция `my_read()` демонстрирует типичную структуру:

```c
int my_read(const char *path, char *buf, size_t size, off_t offset, 
            struct fuse_file_info *fi) {
    int res;

    // Читаем из реального файла через сохранённый file descriptor
    res = pread(fi->fh, buf, size, offset);
    if (res == -1) {
        res = -errno;
    }

    // Логируем операцию
    log_operation("READ", path, res);
    return res;
}
```

#### 3.1.3 Команды для запуска

```bash
# Сборка проекта
make

# Создание директорий
mkdir -p /tmp/fuse_source /tmp/fuse_mount

# Запуск FUSE в foreground режиме
./myfuse /tmp/fuse_source /tmp/fuse_mount -f
```

#### 3.1.4 Результаты компиляции

```
Версия FUSE: 2.9.9
Размер бинарника: 18 KB (до strip), 15 KB (после strip)
Компиляция: Без ошибок и предупреждений
```

### 3.2 Базовое тестирование

#### 3.2.1 Проверка монтирования

```bash
$ mount | grep fuse
fuse_source on /tmp/fuse_mount type fuse.fuse_source 
  (rw,nosuid,nodev,relatime,user_id=1000,group_id=1000)
```

✅ Файловая система успешно смонтирована.

#### 3.2.2 Тест: Чтение файлов

**Команды:**
```bash
$ ls -la /tmp/fuse_mount/
total 102468
drwxrwxr-x  3 fancurson fancurson      4096 Nov 27 21:50 .
drwxrwxrwt 33 root      root          45056 Nov 27 21:51 ..
-rw-rw-r--  1 fancurson fancurson 104857600 Nov 27 21:50 bigfile
-rw-rw-r--  1 fancurson fancurson        23 Nov 27 21:50 sample.txt
drwxrwxr-x  2 fancurson fancurson      4096 Nov 27 21:50 subdir
-rw-rw-r--  1 fancurson fancurson        27 Nov 27 21:50 test.txt

$ cat /tmp/fuse_mount/test.txt
Hello from FUSE filesystem
```

**Логи операций:**
```
[2025-11-27 21:53:00] GETATTR: / (result: 0)
[2025-11-27 21:53:01] READDIR: / (result: 0)
[2025-11-27 21:53:01] GETATTR: /subdir (result: 0)
[2025-11-27 21:53:01] GETATTR: /bigfile (result: 0)
[2025-11-27 21:53:01] GETATTR: /sample.txt (result: 0)
[2025-11-27 21:53:01] GETATTR: /test.txt (result: 0)

[2025-11-27 21:53:13] GETATTR: /test.txt (result: 0)
[2025-11-27 21:53:13] OPEN: /test.txt (result: 0)
[2025-11-27 21:53:13] READ: /test.txt (result: 27)
[2025-11-27 21:53:13] RELEASE: /test.txt (result: 0)
```

✅ Чтение работает корректно. Для команды `cat` вызывается последовательность: GETATTR → OPEN → READ → RELEASE.

#### 3.2.3 Тест: Создание файла

**Команды:**
```bash
$ echo "This is a new file created through FUSE" > /tmp/fuse_mount/newfile.txt
$ cat /tmp/fuse_mount/newfile.txt
This is a new file created through FUSE

$ cat /tmp/fuse_source/newfile.txt
This is a new file created through FUSE
```

**Логи операций:**
```
[2025-11-27 21:54:16] GETATTR: /newfile.txt (result: -2)
[2025-11-27 21:54:16] CREATE: /newfile.txt (result: 0)
[2025-11-27 21:54:16] GETATTR: /newfile.txt (result: 0)
[2025-11-27 21:54:16] WRITE: /newfile.txt (result: 40)
[2025-11-27 21:54:16] RELEASE: /newfile.txt (result: 0)
```

✅ Файл успешно создан через FUSE и доступен в реальной ФС.

#### 3.2.4 Тест: Создание и удаление директории

**Команды:**
```bash
$ mkdir /tmp/fuse_mount/testdir
$ echo "File in new dir" > /tmp/fuse_mount/testdir/file.txt
$ ls -la /tmp/fuse_mount/testdir/
total 12
drwxrwxr-x 2 fancurson fancurson 4096 Nov 27 21:55 .
drwxrwxr-x 4 fancurson fancurson 4096 Nov 27 21:55 ..
-rw-rw-r-- 1 fancurson fancurson   16 Nov 27 21:55 file.txt

$ rm /tmp/fuse_mount/testdir/file.txt
$ rmdir /tmp/fuse_mount/testdir
```

**Логи операций:**
```
[2025-11-27 21:55:43] GETATTR: /testdir (result: -2)
[2025-11-27 21:55:43] MKDIR: /testdir (result: 0)
[2025-11-27 21:55:47] CREATE: /testdir/file.txt (result: 0)
[2025-11-27 21:55:47] WRITE: /testdir/file.txt (result: 16)

[2025-11-27 21:57:01] UNLINK: /testdir/file.txt (result: 0)
[2025-11-27 21:57:05] RMDIR: /testdir (result: 0)
```

✅ Операции с директориями работают корректно.

#### 3.2.5 Тест: Копирование файлов

**Команды:**
```bash
$ cp /tmp/fuse_mount/sample.txt /tmp/fuse_mount/sample_copy.txt
$ diff /tmp/fuse_mount/sample.txt /tmp/fuse_mount/sample_copy.txt
(нет вывода — файлы идентичны)
```

**Логи операций:**
```
[2025-11-27 21:57:34] GETATTR: /sample.txt (result: 0)
[2025-11-27 21:57:34] OPEN: /sample.txt (result: 0)
[2025-11-27 21:57:34] GETATTR: /sample_copy.txt (result: -2)
[2025-11-27 21:57:34] CREATE: /sample_copy.txt (result: 0)
[2025-11-27 21:57:34] READ: /sample.txt (result: 23)
[2025-11-27 21:57:34] WRITE: /sample_copy.txt (result: 23)
[2025-11-27 21:57:34] RELEASE: /sample_copy.txt (result: 0)
[2025-11-27 21:57:34] RELEASE: /sample.txt (result: 0)
```

✅ Команда `cp` работает через FUSE: открывает исходный файл, создаёт новый, читает и записывает данные.

---

## 4. Нагрузочное тестирование

### 4.1 Тест 1: Latency операций (задержка)

**Методика:** Измерение времени выполнения 100 операций каждого типа.

#### Результаты

| Операция | FUSE (avg, ms) | Native FS (avg, ms) | Overhead |
|----------|----------------|---------------------|----------|
| CREATE   | 3.75           | 2.85                | +31%     |
| GETATTR  | 4.95           | ~1.5 (оценка)       | +230%    |
| READ     | 4.54           | ~1.0 (оценка)       | +354%    |
| WRITE    | 0.80           | ~0.5 (оценка)       | +60%     |

**График (текстовое представление):**

```
Latency (ms) per operation:

CREATE   |████████ 3.75 ms (FUSE)
         |██████ 2.85 ms (Native)
         
GETATTR  |██████████ 4.95 ms (FUSE)
         |███ ~1.5 ms (Native)
         
READ     |█████████ 4.54 ms (FUSE)
         |██ ~1.0 ms (Native)
         
WRITE    |██ 0.80 ms (FUSE)
         |█ ~0.5 ms (Native)
```

**Анализ:**
- FUSE добавляет **overhead 31-354%** в зависимости от операции
- Метаданные (GETATTR) страдают сильнее из-за частых context switch
- Операции записи относительно быстрые благодаря буферизации

### 4.2 Тест 2: Throughput (пропускная способность)

**Методика:** Измерение скорости последовательного чтения/записи больших файлов.

#### Результаты

**Запись:**

| Размер файла | FUSE (MB/s) | Native FS (MB/s) | Замедление |
|--------------|-------------|------------------|------------|
| 10 MB        | 70.2        | ~1100            | 15.7x      |
| 50 MB        | 88.5        | ~1100            | 12.4x      |
| 100 MB       | 79.1        | 1100             | 13.9x      |

**Чтение:**

| Размер файла | FUSE (MB/s) | Native FS (MB/s) | Замедление |
|--------------|-------------|------------------|------------|
| 10 MB        | 751         | 2900             | 3.9x       |
| 50 MB        | 697         | 2900             | 4.2x       |
| 100 MB       | 727         | 2900             | 4.0x       |

**График:**

```
Throughput (MB/s):

WRITE (100MB):
FUSE    |████ 79.1 MB/s
Native  |██████████████████████████████████████ 1100 MB/s

READ (100MB):
FUSE    |███████████████ 727 MB/s
Native  |██████████████████████████████████████████ 2900 MB/s
```

**Анализ:**
- **Запись:** FUSE в 13-15 раз медленнее из-за копирования данных через kernel/userspace boundary
- **Чтение:** FUSE в 4 раза медленнее благодаря page cache в ядре (частично компенсирует overhead)
- Производительность стабильна для больших файлов (overhead амортизируется)

### 4.3 Тест 3: IOPS (операции в секунду)

**Методика:** Создание, чтение и удаление 1000 маленьких файлов по ~14 байт.

#### Результаты

| Операция        | FUSE (время, сек) | Native FS (время, сек) | Замедление |
|-----------------|-------------------|------------------------|------------|
| Создание (1000) | 0.189             | 0.047                  | 4.0x       |
| Чтение (1000)   | 3.303             | ~0.1 (оценка)          | 33x        |
| Удаление (1000) | 3.052             | 0.035                  | 87x        |

**График:**

```
Time (seconds) for 1000 operations:

CREATE:
FUSE    |████ 0.189s
Native  |█ 0.047s

READ:
FUSE    |████████████████████████████████████████ 3.303s
Native  |█ ~0.1s

DELETE:
FUSE    |███████████████████████████████████████ 3.052s
Native  |█ 0.035s
```

**Анализ:**
- **IOPS — самая слабая сторона FUSE!**
- Каждая операция требует context switch: userspace ↔ kernel
- Удаление особенно медленное: GETATTR (проверка) + UNLINK для каждого файла
- Для 1000 файлов: 2000 системных вызовов через FUSE протокол
- **Вывод:** FUSE не подходит для workloads с большим количеством мелких файлов

### 4.4 Сводная таблица производительности

| Метрика              | FUSE           | Native FS      | Overhead  |
|----------------------|----------------|----------------|-----------|
| Latency (CREATE)     | 3.75 ms        | 2.85 ms        | +31%      |
| Throughput (Write)   | 79 MB/s        | 1100 MB/s      | 13.9x     |
| Throughput (Read)    | 727 MB/s       | 2900 MB/s      | 4.0x      |
| IOPS (1000 files)    | 3.3 sec        | 0.1 sec        | 33x       |

---

## 5. Особенности реализации

### 5.1 Использование FUSE 2.x вместо FUSE 3

**Причина:** Код изначально разработан для FUSE 2.9.9, доступной в системе.

**Ключевые отличия от FUSE 3:**

| Параметр           | FUSE 2.x                         | FUSE 3.x                           |
|--------------------|----------------------------------|------------------------------------|
| `FUSE_USE_VERSION` | 26                               | 31                                 |
| `getattr()`        | 2 параметра                      | 3 параметра (+`fi`)                |
| `readdir()`        | 5 параметров                     | 6 параметров (+`flags`)            |
| `filler()`         | 4 аргумента                      | 5 аргументов (+`flags`)            |

### 5.2 Не реализованные операции

**`utimens` (установка временных меток):** Не реализовано, поэтому команда `touch` выдаёт ошибку:

```
touch: setting times of '/tmp/fuse_mount/latency_test_1.txt': Function not implemented
```

Это не влияет на создание файлов (файлы создаются успешно), но временные метки не обновляются.

**Другие не реализованные операции:**
- `chmod` / `chown` — изменение прав и владельца
- `truncate` — изменение размера файла
- `symlink` / `readlink` — работа с символическими ссылками
- `rename` — переименование файлов

Эти операции можно добавить при необходимости, реализация тривиальна (проксирование в соответствующие системные вызовы).

### 5.3 Обработка ошибок

Все операции корректно возвращают коды ошибок POSIX:
- `-ENOENT` (-2) — файл не найден
- `-EACCES` (-13) — отказ в доступе
- `-ENOMEM` (-12) — недостаточно памяти
- И т.д.

Пример из логов:
```
[2025-11-27 21:52:12] GETATTR: /.Trash (result: -2)
[2025-11-27 21:54:16] GETATTR: /newfile.txt (result: -2)
```

---

## 6. Ответы на контрольные вопросы

### Архитектура файловых систем

**1. Что такое VFS (Virtual File System) и зачем она нужна?**

VFS — это абстрактный слой в ядре Linux, который предоставляет единый интерфейс для работы с различными файловыми системами. Без VFS каждое приложение должно было бы реализовывать поддержку каждого типа ФС (ext4, btrfs, NFS и т.д.). VFS позволяет использовать одни и те же системные вызовы (`open`, `read`, `write`) независимо от типа файловой системы.

**2. Объясните разницу между inode, dentry и file descriptor.**

- **inode** — структура в ФС, хранящая метаданные файла: размер, права доступа, временные метки, указатели на блоки данных. Каждый файл имеет уникальный inode number.
- **dentry** (directory entry) — запись в кеше директорий, связывающая имя файла с его inode. Ускоряет поиск файлов по пути.
- **file descriptor** — целочисленный идентификатор открытого файла в процессе. Указывает на структуру `struct file` в ядре, которая содержит текущую позицию чтения/записи и флаги доступа.

**3. Что хранится в структуре `struct inode`?**

- Размер файла (`i_size`)
- Права доступа (`i_mode`)
- Владелец и группа (`i_uid`, `i_gid`)
- Временные метки: `i_atime` (последний доступ), `i_mtime` (изменение данных), `i_ctime` (изменение метаданных)
- Количество ссылок (`i_nlink`)
- Указатели на блоки данных (`i_blocks`)
- Операции для работы с файлом (`i_op`, `i_fop`)

**4. Что такое superblock и какую информацию он содержит?**

Superblock — это структура, описывающая файловую систему в целом. Содержит:
- Размер блока данных
- Общее количество блоков и свободных блоков
- Указатель на root inode
- Magic number (идентификатор типа ФС)
- Флаги монтирования (read-only, noexec и т.д.)

**5. Как работает кеш dentry и зачем он нужен?**

Dentry cache (dcache) кеширует результаты поиска файлов по пути. Когда приложение обращается к `/home/user/file.txt`, ядро должно разрешить каждый компонент пути: `/` → `home` → `user` → `file.txt`. Dcache хранит эти соответствия в памяти, избегая повторных обращений к диску. Это критично для производительности, так как многие приложения обращаются к одним и тем же файлам многократно.

---

### FUSE

**6. Как FUSE взаимодействует с ядром Linux?**

FUSE использует драйвер в ядре (`/dev/fuse`) и протокол передачи сообщений. Когда VFS получает системный вызов для FUSE ФС, драйвер формирует запрос и отправляет его через устройство `/dev/fuse` в userspace. Процесс FUSE (демон) читает запрос, обрабатывает его (вызывает соответствующий callback), и отправляет ответ обратно через `/dev/fuse`. Ядро возвращает результат приложению.

**7. Опишите путь системного вызова `read()` в FUSE FS.**

1. Приложение вызывает `read(fd, buf, size)`
2. Системный вызов попадает в ядро → VFS
3. VFS определяет, что файл принадлежит FUSE ФС
4. FUSE kernel module формирует FUSE_READ запрос
5. Запрос отправляется через `/dev/fuse` в userspace
6. libfuse читает запрос и вызывает `my_read()`
7. `my_read()` выполняет реальный `pread()` на файле
8. Данные копируются обратно через `/dev/fuse`
9. Ядро копирует данные в буфер приложения
10. `read()` возвращает количество прочитанных байт

**8. Почему FUSE работает медленнее нативных kernel FS?**

Три основные причины:
- **Context switch:** Каждая операция требует переключения kernel ↔ userspace
- **Копирование данных:** Данные копируются дважды: диск → kernel → userspace → kernel → приложение
- **Отсутствие оптимизаций:** Kernel ФС могут использовать прямой доступ к страничному кешу, zero-copy техники и специальные оптимизации ядра

**9. Какие преимущества дает разработка FS в userspace?**

- **Безопасность:** Ошибка в FUSE не вызовет kernel panic
- **Простота разработки:** Можно использовать обычные библиотеки, отладчики (gdb), sanitizers
- **Быстрая итерация:** Не нужна перезагрузка системы после изменений
- **Портируемость:** FUSE доступна на Linux, macOS, FreeBSD
- **Гибкость:** Можно реализовать экзотическую логику (шифрование, сетевые ФС, виртуальные ФС)

**10. Приведите примеры популярных FUSE файловых систем.**

- **sshfs** — монтирование удалённой директории через SSH
- **GCS FUSE** — доступ к Google Cloud Storage как к локальной ФС
- **s3fs** — монтирование Amazon S3 bucket
- **EncFS / gocryptfs** — шифрующ

## 7. Использование AI

В данной работе AI был применен для следующих целей:
- Структурирование кода, дополнение его комментариями
- Помощь в сборе статистики и написании отчета