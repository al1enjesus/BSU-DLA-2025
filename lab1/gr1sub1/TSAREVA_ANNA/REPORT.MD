## Дата и среда выполнения
- **Дата выполнения:** 22.09.2025  
- **Среда выполнения:** Ubuntu 22  

---

## Задание A

**Цель:** поиск наиболее частых упоминаний в логах  

**Команда:**
```bash
cat /var/log/syslog \
| tr -cs '[:alnum:]' '\n' \
| tr '[:upper:]' '[:lower:]' \
| sort \
| uniq -c \
| sort -nr \
| head -n 5
Результат выполнения:

yaml
Копировать код
9389 00
8631 03
8496 2025
8494 anna
5199 signal
Вывод:
Команда извлекает все слова из системного лога, подсчитывает количество повторений каждого слова, сортирует их по убыванию частоты и выводит 5 самых часто встречающихся слов в файле.

Задание B
Цель: поиск 10-ти самых частых неудач входа

Команда:

bash
Копировать код
cat /var/log/auth.log \
| grep -E "Failed|Invalid" \
| grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' \
| sed -E 's/([0-9]+\.[0-9]+\.[0-9]+\.)[0-9]+/\1x/g' \
| sort \
| uniq -c \
| sort -nr \
| head -10
Результат выполнения:

scss
Копировать код
(пусто)
Вывод:
Команда не вывела результата, так как в логах отсутствуют неудачные попытки входа.

Задание C
Цель: вывод топа и количества наиболее часто устанавливаемых приложений

Команда:

bash
Копировать код
grep " install " /var/log/dpkg.log \
| awk '{print $4}' \
| cut -d ':' -f 1 \
| sort \
| uniq -c \
| sort -nr \
| head -n 10
Результат выполнения:

Копировать код
1 vim-runtime
1 vim
1 liberror-perl
1 htop
1 git-man
1 git
Вывод:
Команда вывела список установленных пакетов и их частоту. В данном случае каждый из пакетов (vim-runtime, vim, liberror-perl, htop, git-man, git) был установлен один раз. Команда работает корректно и отражает реальную частоту установок.

Проверка корректности
Все команды тестировались на архивных лог-файлах.

При наличии данных команды показывают корректный результат.

Использование AI
AI применялся для:

проверки правильности синтаксиса и логики команд,

подбора альтернативных решений и инструментов для обработки лог-файлов.