# Лабораторная 2: Продвинутые процессы Linux

## Задание A: Мини-супервизор с воркерами

### Сборка

Для сборки проекта используйте команду `make` в корневой директории.

### Конфигурация (config.ini)

Супервизор считывает конфигурацию из файла `config.ini`. Формат:

```ini
workers=N        
work_heavy_us=N  
sleep_heavy_us=N 
work_light_us=N  
sleep_light_us=N 