# Отчет по лабораторной работе 2
Кушмар Элина 9 группа

## Выполненные задания

### Задание A: Мини-супервизор с воркерами

#### Цель
Реализовать систему управления процессами с graceful shutdown, reload и переключением режимов работы.

---

#### Шаги выполнения

1. **Запуск супервизора:**

```bash
python3 supervisor.py --config config.json
```

2. **Проверка запущенных процессов:**

```bash
ps aux | grep supervisor.py
ps aux | grep worker.py | grep -v grep
```

**Вывод:**

- Супервизор: `PID 21344`  
- Воркеры: `PID 21346, 21347, 21348` (3 процесса)  
- Все процессы работают в тяжелом режиме (`%CPU ~87%`)

---

3. **Тестирование сигналов управления:**

- **SIGUSR1 / SIGUSR2 — переключение режимов:**

```bash
kill -USR1 21344  # Легкий режим
kill -USR2 21344  # Тяжелый режим
```

- **SIGHUP — graceful reload:**

```bash
kill -HUP 21344
```

**Результат:**  
Супервизор перезапустил воркеров с новыми PID (`21682, 21684, 21685, 21686, 21687`).

- **Тест автоперезапуска:**

```bash
kill -KILL 21683  # Убиваем воркера
sleep 3
ps aux | grep worker.py | grep -v grep
```

**Результат:**  
Появился новый воркер `PID 21704` — автоперезапуск работает.

- **SIGTERM — graceful shutdown:**

```bash
kill -TERM 21344
```

---

#### Результаты тестирования сигналов

**Логи graceful shutdown:**

```
SUPERVISOR[21344] 23:38:46 - INFO - Received signal 15, initiating graceful shutdown
SUPERVISOR[21344] 23:38:46 - INFO - Starting graceful shutdown
SUPERVISOR[21344] 23:38:46 - INFO - Sent signal 15 to worker 21682
WORKER[21682] 23:38:46 - INFO - Received signal 15, initiating graceful shutdown
...
WORKER[21686] 23:38:46 - INFO - Final stats: 5741 iterations, total work: 51912.15ms, total sleep: 7433.75ms
WORKER[21686] 23:38:46 - INFO - Worker shutdown complete
```

---

#### Выводы по заданию A

- Супервизор порождает и управляет N воркерами  
- Корректная обработка `SIGTERM` / `SIGINT` / `SIGHUP` / `SIGUSR1` / `SIGUSR2`  
- Graceful shutdown ≤ 5 секунд  
- Graceful reload с перезапуском воркеров  
- Автоперезапуск упавших воркеров с ограничением частоты  
- Корректный "подбор" зомби-процессов  


---

### Задание B: Планирование - влияние nice

#### Цель
Исследовать влияние значения nice на распределение CPU между процессами в macOS.

---

#### Методика
- Запущено **3 идентичных воркера** с CPU-нагрузкой (~90% каждый).  
- Изменение nice:  
  - `PID 22600`: `0 → 10 → 20`  
  - Остальные: `nice = 0`  
- Мониторинг проводился в течение **5+ минут**.

---

#### Результаты

**Данные мониторинга CPU:**

| Время | PID   | Nice | %CPU  | Изменение         |
|-------|-------|------|-------|-------------------|
| 1:59  | 22600 | 10   | 87.3  | После nice=10     |
| 1:59  | 22601 | 0    | 87.3  | Контроль          |
| 2:30  | 22600 | 10   | 88.4  | Стабильно         |
| 2:30  | 22601 | 0    | 88.4  | Стабильно         |
| 4:01  | 22600 | 20   | 89.8  | После nice=20     |
| 4:01  | 22601 | 0    | 89.7  | Контроль          |
| 4:59  | 22600 | 20   | 87.9  | Финальные         |
| 4:59  | 22601 | 0    | 89.5  | Финальные         |

**Данные памяти (vm_stat):**
- Свободная память: **2-9 MB** (стабильно низкая)  
- Активная память: **97-116 MB** (умеренная нагрузка)  
- Сжатая память: **587-634 MB** (эффективное использование)  
- Swap: минимальный (система не испытывает нехватки памяти)

---

#### Анализ и выводы

1. **Влияние nice на macOS**  
   - Результат: **незначительное влияние**  
   - Наблюдения: все процессы получают ~88-89% CPU независимо от nice  
   - Причина: планировщик macOS равномерно распределяет ресурсы при избытке CPU  

2. **Стабильность системы**  
   - Память: стабильное состояние, эффективное сжатие  
   - CPU: равномерное распределение между процессами  
   - Нагрузка: система справляется с высокой CPU-нагрузкой  

3. **Особенности macOS**  
   - Отсутствует CPU-аффинити (ограничение платформы)  
   - Значения nice имеют меньший вес, чем в Linux  
   - Приоритеты (PRI) остаются одинаковыми независимо от nice  

---

#### Ответы на вопросы

- **Как nice влияет на планирование?**  
  На macOS влияние минимально. При достаточных ресурсах планировщик равномерно распределяет CPU время.

- **CPU-аффинити на macOS**  
  Недоступно. На Linux позволяет закреплять процессы за ядрами, но может привести к дисбалансу.

---

#### Итог
- Задание выполнено: исследовано влияние nice на macOS  
- Результат: на данной конфигурации эффект незначителен  
- Причина: избыток CPU ресурсов, особенности планировщика macOS  

Система стабильно работает под нагрузкой, все процессы корректно завершены.  
